apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Required when using --ceph-mon-configmap-namespace. Not in default (Role must stay in rook-ceph).
resources:
- role.yaml
- role_binding.yaml
